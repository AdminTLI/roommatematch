/** URL query value for middleware → sign-in when the session was revoked (e.g. login elsewhere). */
export const SESSION_TERMINATED_ERROR_PARAM = 'session_terminated'

export const SESSION_TERMINATED_MESSAGE =
  'You have been logged out because your account was accessed from another device.'
