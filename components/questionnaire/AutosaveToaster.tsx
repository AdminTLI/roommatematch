'use client'

export function AutosaveToaster({ show: _show }: { show: boolean }) {
  // Keep autosave toast API stable, but intentionally render nothing.
  // Questionnaire pages already show "Last saved" inline feedback.
  return null
}


